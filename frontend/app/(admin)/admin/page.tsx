import React from 'react';

/**
 * Placeholder scaffold for the Admin workspace (S0-T12).
 * The real admin control center is built in a later sprint (S4). This stub only
 * proves the guarded route group resolves and renders inside AppShell.
 */
export default function AdminHomePlaceholder() {
  return (
    <div className="space-y-2">
      <h2 className="text-2xl font-bold text-foreground">Admin workspace</h2>
      <p className="text-muted-foreground">
        Admin screens are coming in a later sprint.
      </p>
    </div>
  );
}
