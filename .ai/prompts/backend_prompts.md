# Backend prompts

Starter prompts for agents. Always reference the relevant doc and cite the PRD section.

- "Add DRF endpoint under /api/v1/ for <X> with serializer + service + selector + tests."
- "Enforce RBAC + tenant scoping on <endpoint>; add a test that cross-tenant access is denied."
- "Implement credit reserve/commit/release for <AI op> with select_for_update."
