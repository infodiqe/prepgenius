# Architecture prompts

Starter prompts for agents. Always reference the relevant doc and cite the PRD section.

- "Implement <feature> as a service in app <X>; cite PRD v4 §. No logic in views."
- "Add a Celery task for <job> in the correct queue; make it idempotent."
- "Extend ai_gateway with <provider> in the fallback chain via config only."
